import * as vscode from 'vscode';
import axios from 'axios';
const errorMessage = "Smart Coder: Prior to utilizing the Smart Coder extension, kindly configure the API key for ChatGPT."

export function activate(context: vscode.ExtensionContext) {
  console.log('Congratulations, your extension "smartcoder" is now active!');
}